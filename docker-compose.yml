version: "3.9"
services:
  mongo:
    image: mongo
    ports:
      - 27018:27018
  web:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    links:
      - mongo

